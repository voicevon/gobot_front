# Install twh-client-agent 
```
pip install numpy
pip install opencv-contrib-python
pip install mss
pip install pillow
```
# Install Server 
```
pip install flask
# pip install flask-wtf
pip install tinydb
pip install VonPylib
pip install paho-mqtt
pip install flask-sock
```